# Codex MCP Integration Example

This example focuses on the narrow topic of connecting Anydocs MCP to Codex and working through an MCP-first authoring flow.

Use it when you want a smaller, task-oriented reference instead of the fuller `codex-authoring-docs` site.

## What This Example Covers

- Codex MCP setup
- MCP-first writing workflow
- smaller guide set for fast orientation

## Quick Start

```bash
node --experimental-strip-types packages/cli/src/index.ts build examples/codex-mcp-docs
node --experimental-strip-types packages/cli/src/index.ts preview examples/codex-mcp-docs
```
