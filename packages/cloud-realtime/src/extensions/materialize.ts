// STUB — wired in Story C2/C6 (EARLY SPIKE flagged in the architecture/readiness report).
// Debounced materialization of the Y.Doc into canonical doc-content-v1 (jsonb) on idle/save,
// reusing the editor's doc-content-v1 <-> Plate converters. doc-content-v1 stays canonical;
// the Y.Doc is runtime-only.
export const MATERIALIZE_BRIDGE_TODO =
  'Story C2/C6 SPIKE: Y.Doc <-> doc-content-v1 bridge (debounced materialize on idle)';
