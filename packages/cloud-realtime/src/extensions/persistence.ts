// STUB — wired in Story C6. Persist the Y.Doc binary into Postgres `ydoc_states`
// (via @anydocs/cloud-core) on the Hocuspocus `onStoreDocument` / `onLoadDocument` hooks.
export const PERSISTENCE_EXTENSION_TODO = 'Story C6: persist Y.Doc binary to ydoc_states';
